bugs

- in Create/Edit Employee Employee level is not loading in dropdown
- in cohort not showing newly created cohort [BE issue].
- in employee login : Employee details should be shown 
- in problem facing in facing problem in SBU , BU and department  there is not edit and delete button 
- in department after adding Problem facing it's not showing in edit or view 
- JT level not loading in Create job title 
- in edit org details change logo button is not working.
- in edit org details clicking on next button from first page throws 
	  ```Runtime Error[](https://nextjs.org/docs/app/building-your-application/configuring/debugging#server-side-code "Learn more about enabling Node.js inspector for server code with Chrome DevTools")
	
	Objects are not valid as a React child (found: [object Error]). If you meant to render a collection of children, use an array instead.
- I selected a filter IN BU  which resulted in showing empty card but filter dropdown is gone after that
- I selected a filter IN SBU  which resulted in showing empty card but filter dropdown is gone after that
- I selected a filter IN DEP  which resulted in showing empty card but filter dropdown is gone after that
- I selected a filter IN JT  which resulted in showing empty card but filter dropdown is gone after that
- there is not filters in employee module.
- I selected a filter IN Teams   which resulted in showing empty card but filter dropdown is gone after that.
- I selected a filter IN Cohort  which resulted in showing empty card but filter dropdown is gone after that
- No filters in User Permissions 